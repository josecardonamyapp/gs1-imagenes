version: 0.2

phases:
  install:
    commands:
      - npm install -g @angular/cli
      - npm install
  build:
    commands:
      - ng build
      - ls -la
artifacts:
  files:
    - '**/*'
  base-directory: dist/flexy